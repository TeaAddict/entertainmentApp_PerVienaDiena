export const getMovies = (req, res) => {
  res.json({ message: "Got one movie!" });
};

export const getMovie = (req, res) => {
  res.json({ message: "Got all movie!" });
};

export const updateMovie = (req, res) => {
  res.json({ message: "Update movie!" });
};
